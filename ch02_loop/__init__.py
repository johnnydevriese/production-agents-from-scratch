"""Chapter 2 checkpoint — the autopilot as a loop around the stateless model.

Two read-only tools (`lookup_invoice`, `check_budget`) wired into an
observe→think→act→observe loop that prints every step.
"""
