"""Chapter 21 checkpoint — the statistics that keep an eval honest.

A pass rate is an estimate of a hidden probability, not a measurement. Put a
Wilson band on it (`intervals.py`); compare two versions on the same cases with
McNemar's paired test (`compare.py`).
"""
